# TODO: Fix Backend Errors

## Tasks to Complete

- [x] Update `app.js` to increase body-parser limit to 10mb
- [x] Update `course.controller.js` to normalize course level to title case
- [x] Fix `getCourses` method to use `Course.find()` instead of `findAll()`
- [x] Change `createdBy` to `instructorId` in `createCourse` method
- [x] Test the fixes by running the server
